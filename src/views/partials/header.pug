header 
  a(href="/").header__logo
    i.fab.fa-youtube
  nav 
    ul           
      li 
        a(href="/search")
          i.fas.fa-search
      if loggedIn 
        li
          a(href="/videos/upload") Upload Video
        li 
          a(href="/users/edit") Edit Profilet          
        li 
          a(href="/users/logout") Logout
        li 
          a(href=`/users/${loggedInUser._id}`)
            if loggedInUser.avatarUrl === undefined
              span.header__emoji 😀
            else  
              img(src="/" + loggedInUser.avatarUrl).header__avatar
      else
        li 
          a(href="/login").header__btn Login    
        li 
          a(href="/join") Join
        
